import { Typography } from "@mui/material"

const Header = () => {
    return (
        <Typography variant='h2' p={2} sx={{ bgcolor: '#DBE2EF', color: '#112D4E', fontWeight: 'bold' }} align='center'>
            Note Maker
        </Typography>
        )
}

export default Header